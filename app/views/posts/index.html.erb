<h1>Blog Posts</h1>
<% @posts.each do |post| %>
	<div class="well">
	<h3><%= post.title %></h3>
	<%= image_tag post.image.url(:thumb), class: 'image-show' if post.image? %>
	<%= link_to "Read More", post_path(post), :class => 'btn btn-default' %>
	</div>
	<hr>
<% end %>
